---
import { withBase } from "../lib/base.js";

interface Props {
  name: string;
  price: string;
  tag: string;
  blurb: string;
  temp: string;
  milk: string;
  image: string;
  signature?: string;
}

const { name, price, tag, blurb, temp, milk, image, signature } = Astro.props;
---

<article class="drink-card">
  <div class="drink-media">
    <img src={withBase(image)} alt={name} loading="lazy" />
  </div>
  <div class="drink-content">
    <div class="drink-heading">
      <div>
        <p class="drink-tag">{tag}</p>
        <h3>{name}</h3>
      </div>
      <span class="drink-price">{price}</span>
    </div>
    <p class="drink-blurb">{blurb}</p>
    <dl class="drink-meta">
      <div><dt>Serve</dt><dd>{temp}</dd></div>
      <div><dt>Choice</dt><dd>{milk}</dd></div>
    </dl>
    {signature && (
      <details class="drink-signature">
        <summary>What makes it this drink</summary>
        <p>{signature}</p>
      </details>
    )}
  </div>
</article>
